<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <artifactId>archetype</artifactId>
  <groupId>it.cnr.si</groupId>
  <version>0.13-SNAPSHOT</version>
  <name>CNR USID Archetype</name>

  <packaging>pom</packaging>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <nexus.url>http://maven.si.cnr.it</nexus.url>
    <java.version>1.7</java.version>
  </properties>

  <repositories>
    <repository>
      <id>cnr</id>
      <name>cnr public</name>
      <url>${nexus.url}/content/groups/public</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>cnr-snapshots</id>
      <name>cnr public</name>
      <url>${nexus.url}/content/groups/public-snapshots</url>
      <releases>
        <enabled>false</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>cnr</id>
      <name>cnr public</name>
      <url>${nexus.url}/content/groups/public</url>
    </pluginRepository>
    <pluginRepository>
      <id>cnr-snapshots</id>
      <name>cnr snapshots</name>
      <url>${nexus.url}/content/groups/public-snapshots</url>
      <releases>
        <enabled>false</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </pluginRepository>
  </pluginRepositories>


  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>2.5.1</version>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-source-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-sources</id>
            <phase>deploy</phase>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-javadoc-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-javadocs</id>
            <phase>deploy</phase>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <!-- explicitly define maven-deploy-plugin after other to force exec order -->
        <artifactId>maven-deploy-plugin</artifactId>
        <version>2.7</version>
        <executions>
          <execution>
            <id>deploy</id>
            <phase>deploy</phase>
            <goals><goal>deploy</goal></goals>
          </execution>
                    <execution>
                      <id>default-deploy</id>
                     <phase>deploy</phase>
                     <goals>
                   <goal>deploy</goal>
                </goals>
                <configuration>
                <skip>true</skip>
              </configuration>
          </execution>

        </executions>
      </plugin>
    </plugins>
  </build>

  <distributionManagement>
    <!-- Publish the versioned releases here -->
    <repository>
      <id>buildandtest</id>
      <name>nexus</name>
      <url>${nexus.url}/content/repositories/releases</url>
    </repository>

    <!-- Publish the versioned releases here -->
    <snapshotRepository>
      <id>buildandtest</id>
      <name>nexus</name>
      <url>${nexus.url}/content/repositories/snapshots</url>
    </snapshotRepository>
  </distributionManagement>

  <scm>
    <connection>scm:svn:svn+ssh://svnselezioni.si.cnr.it/DATA/svnroot/svn-documentale/trunk/archetype</connection>
    <developerConnection>scm:svn:svn+ssh://svnselezioni.si.cnr.it/DATA/svnroot/svn-documentale/trunk/archetype</developerConnection>
  </scm>

</project>
